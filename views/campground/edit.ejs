<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Campground</title>
</head>

<body>
  <h1>Edit Campground</h1>
  <form action="/campground/<%= campground.id%>?_method=PUT" method="post">
    <div>
      <label for="title">Title: </label>
      <input type="text" id="title" name="campground[title]" value="<%= campground.title %>">
      <!--* name attribute of title - is grouped under campground  -->
    </div>
    <div>
      <label for="title">Location: </label>
      <input type="text" id="location" name="campground[location]" value="<%= campground.location %>">
      <!--* name attribute of location - is grouped under campground  -->
    </div>
    <button>Edit Campground</button>
  </form>
  <a href="/campground/<%= campground.id%>">Cancel</a>
</body>

</html>